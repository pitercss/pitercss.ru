---
layout: layouts/base.njk
templateClass: tmpl-post
---

<h1>pitercss_meetup {{ title }} {{ location | safe }}</h1>

{%- if image -%}
  <img src="{{ image }}" alt="{{ post.data.title }}">
{%- endif -%}


{{ content | safe }}
<p><a href="{{ '/' | url }}">← Главная</a></p>
